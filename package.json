{
  "name": "report-generator",
  "version": "1.0.0",
  "description": "A local-first report automation toolkit that turns Markdown into polished, repeatable documents.",
  "scripts": {
    "build": "node scripts/build.js",
    "preview-build": "node scripts/preview_build.js",
    "preview": "node preview-server.js",
    "combine": "node scripts/combine_pdfs.js",
    "finalize": "npm run build && npm run combine",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "GPL-3.0-or-later",
  "type": "commonjs",
  "dependencies": {
    "chokidar": "^5.0.0",
    "express": "^5.2.1",
    "pdf-lib": "^1.17.1",
    "pdfjs-dist": "^6.2.108",
    "ws": "^8.21.0"
  }
}
